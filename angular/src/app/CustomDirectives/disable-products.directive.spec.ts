import { DisableProductsDirective } from './disable-products.directive';

describe('DisableProductsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableProductsDirective();
    expect(directive).toBeTruthy();
  });
});
